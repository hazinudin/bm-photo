apiVersion: v1
kind: Secret
metadata:
  name: bm-photo-secrets
  namespace: bm-photo
  labels:
    app.kubernetes.io/name: bm-photo
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  DB_USERNAME: "postgres"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  GOOGLE_APPLICATION_CREDENTIALS: |
    PLACEHOLDER - Add your GCP service account JSON key content here
    This should be the full JSON content of your service account credentials
